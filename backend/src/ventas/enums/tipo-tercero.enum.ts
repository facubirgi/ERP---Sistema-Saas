/**
 * ENUM: Tipo de Tercero
 *
 * Define si el tercero es un cliente o proveedor.
 * Un tercero puede tener ambos roles simultáneamente.
 */
export enum TipoTercero {
  CLIENTE = 'CLIENTE',
  PROVEEDOR = 'PROVEEDOR',
}
