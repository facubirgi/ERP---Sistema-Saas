[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# Redirecciones para el enrutamiento de Next.js
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
